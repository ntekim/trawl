package main

import (
	"fmt"
	"log"
)

func main() {
	fmt.Println("🛰️ Trawl Engine starting...")
    // In the future, this will initialize the config and start the worker pool
    log.Println("Ready to capture data.")
}